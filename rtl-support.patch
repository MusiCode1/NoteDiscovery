From cb2fb8552bebdce1d64c2b574952c5bc033881ba Mon Sep 17 00:00:00 2001
From: MusiCode1 <musicode3@gmail.com>
Date: Mon, 23 Mar 2026 19:06:28 +0000
Subject: [PATCH] added RTL support with Hebrew locale and dir=auto for user
 content

---
 backend/export.py   |  22 ++--
 backend/main.py     |   6 +-
 frontend/app.js     |  49 +++++---
 frontend/index.html | 190 ++++++++++++++++------------
 frontend/login.html |  11 +-
 locales/he-IL.json  | 295 ++++++++++++++++++++++++++++++++++++++++++++
 6 files changed, 470 insertions(+), 103 deletions(-)
 create mode 100644 locales/he-IL.json

diff --git a/backend/export.py b/backend/export.py
index 1020bb3..76d0712 100644
--- a/backend/export.py
+++ b/backend/export.py
@@ -300,7 +300,8 @@ def generate_export_html(
     title: str,
     content: str,
     theme_css: str,
-    is_dark: bool = False
+    is_dark: bool = False,
+    locale: str = 'en-US'
 ) -> str:
     """
     Generate a standalone HTML document for a note.
@@ -327,8 +328,12 @@ def generate_export_html(
     highlight_theme = 'github-dark' if is_dark else 'github'
     mermaid_theme = 'dark' if is_dark else 'default'
     
+    # RTL support
+    rtl_locales = ['he-IL', 'ar-SA', 'ar-EG', 'fa-IR', 'ur-PK']
+    dir_attr = 'rtl' if locale in rtl_locales else 'ltr'
+    
     html = f'''<!DOCTYPE html>
-<html lang="en">
+<html lang="{locale}" dir="{dir_attr}">
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
@@ -424,8 +429,7 @@ def generate_export_html(
             margin: 0;
             padding: 2rem;
             max-width: 900px;
-            margin-left: auto;
-            margin-right: auto;
+            margin-inline: auto;
             background-color: var(--bg-primary, #ffffff);
             color: var(--text-primary, #333333);
         }}
@@ -504,13 +508,13 @@ def generate_export_html(
         .markdown-preview blockquote {{
             margin: 1em 0;
             padding: 0 1em;
-            border-left: 4px solid var(--accent-primary, #0366d6);
+            border-inline-start: 4px solid var(--accent-primary, #0366d6);
             color: var(--text-secondary, #6a737d);
         }}
         
         .markdown-preview ul,
         .markdown-preview ol {{
-            padding-left: 2em;
+            padding-inline-start: 2em;
             margin: 1em 0;
         }}
         
@@ -528,7 +532,7 @@ def generate_export_html(
         .markdown-preview td {{
             border: 1px solid var(--border-color, #e1e4e8);
             padding: 0.5em 1em;
-            text-align: left;
+            text-align: start;
         }}
         
         .markdown-preview th {{
@@ -544,7 +548,7 @@ def generate_export_html(
         
         /* Task list styling */
         .markdown-preview input[type="checkbox"] {{
-            margin-right: 0.5em;
+            margin-inline-end: 0.5em;
         }}
         
         /* Enhanced Shell/Bash Syntax Highlighting */
@@ -627,7 +631,7 @@ def generate_export_html(
         .copy-btn {{
             position: absolute;
             top: 0.5rem;
-            right: 0.5rem;
+            inset-inline-end: 0.5rem;
             padding: 0.25rem 0.5rem;
             font-size: 0.75rem;
             background-color: var(--bg-secondary, #e1e4e8);
diff --git a/backend/main.py b/backend/main.py
index 8331ad3..8f53b61 100644
--- a/backend/main.py
+++ b/backend/main.py
@@ -1642,11 +1642,15 @@ async def view_shared_note(request: Request, token: str):
         title = Path(note_path).stem
         
         # Generate HTML
+        # Get locale from query parameter or cookie for RTL support
+        locale = request.query_params.get('locale', request.cookies.get('locale', 'en-US'))
+        
         html_content = generate_export_html(
             title=title,
             content=content_with_links,
             theme_css=theme_css,
-            is_dark=is_dark
+            is_dark=is_dark,
+            locale=locale
         )
         
         return HTMLResponse(content=html_content)
diff --git a/frontend/app.js b/frontend/app.js
index 354e0c0..edb44cd 100644
--- a/frontend/app.js
+++ b/frontend/app.js
@@ -204,6 +204,12 @@ function noteApp() {
         // Translations loaded from backend (preloaded before Alpine init via window.__preloadedTranslations)
         translations: window.__preloadedTranslations || {},
         
+        // RTL support
+        RTL_LOCALES: ['he-IL', 'ar-SA', 'ar-EG', 'fa-IR', 'ur-PK'],
+        get isRTL() {
+            return this.RTL_LOCALES.includes(this.currentLocale);
+        },
+        
         // Syntax highlighting
         syntaxHighlightEnabled: false,
         syntaxHighlightTimeout: null,
@@ -484,6 +490,7 @@ function noteApp() {
             await this.loadAvailableLocales();
             // Note: Translations are preloaded synchronously before Alpine init (see index.html)
             // loadLocale() is only called when user changes language from settings
+            this.applyDocumentDirection();
             await this.loadNotes();
             await this.loadSharedNotePaths();
             await this.loadTemplates();
@@ -1083,6 +1090,14 @@ function noteApp() {
         // Change locale and reload translations
         async changeLocale(localeCode) {
             await this.loadLocale(localeCode);
+            this.applyDocumentDirection();
+        },
+        
+        // Apply RTL/LTR direction to document based on current locale
+        applyDocumentDirection() {
+            const dir = this.isRTL ? 'rtl' : 'ltr';
+            document.documentElement.setAttribute('dir', dir);
+            document.documentElement.setAttribute('lang', this.currentLocale);
         },
         
         // ==================== END INTERNATIONALIZATION ====================
@@ -1880,18 +1895,18 @@ function noteApp() {
                         <div class="flex items-center gap-1">
                             <button 
                                 class="flex-shrink-0 w-4 h-4 flex items-center justify-center"
-                                style="color: var(--text-tertiary); cursor: pointer; transition: transform 0.2s; pointer-events: none; margin-left: -5px; ${isExpanded ? 'transform: rotate(90deg);' : ''}"
+                                style="color: var(--text-tertiary); cursor: pointer; transition: transform 0.2s; pointer-events: none; margin-inline-start: -5px; ${isExpanded ? 'transform: rotate(90deg);' : ''}"
                             >
                                 <svg width="12" height="12" viewBox="0 0 16 16" fill="currentColor">
                                     <path d="M6 4l4 4-4 4V4z"/>
                                 </svg>
                             </button>
                             <span class="flex items-center gap-1 flex-1" style="overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-weight: 500; pointer-events: none;" title="${esc(folder.name)}">
-                                <span>${esc(folder.name)}</span>
+                                <span dir="auto">${esc(folder.name)}</span>
                                 ${folder.notes.length === 0 && (!folder.children || Object.keys(folder.children).length === 0) ? `<span class="text-xs" style="color: var(--text-tertiary); font-weight: 400;">(${this.t('folders.empty')})</span>` : ''}
                             </span>
                         </div>
-                        <div class="hover-buttons flex gap-1 transition-opacity absolute right-2 top-1/2 transform -translate-y-1/2" style="opacity: 0; pointer-events: none; background: linear-gradient(to right, transparent, var(--bg-hover) 20%, var(--bg-hover)); padding-left: 20px;" onclick="event.stopPropagation()">
+                        <div class="hover-buttons flex gap-1 transition-opacity absolute end-2 top-1/2 transform -translate-y-1/2" style="opacity: 0; pointer-events: none; background: linear-gradient(to right, transparent, var(--bg-hover) 20%, var(--bg-hover)); padding-inline-start: 20px;" onclick="event.stopPropagation()">
                             <button 
                                 data-path="${esc(folder.path)}"
                                 onclick="window.$root.handleNewItemClick(this, event)"
@@ -1925,7 +1940,7 @@ function noteApp() {
             
             // If expanded, render folder contents (child folders + notes)
             if (isExpanded) {
-                html += `<div class="folder-contents" style="padding-left: 10px;">`;
+                html += `<div class="folder-contents" style="padding-inline-start: 10px;">`;
                 
                 // First, render child folders (if any)
                 if (folder.children && Object.keys(folder.children).length > 0) {
@@ -1962,7 +1977,7 @@ function noteApp() {
             
             // Share icon for shared notes
             const isShared = !isMediaFile && this.isNoteShared(note.path);
-            const shareIcon = isShared ? '<svg title="Shared" style="display: inline-block; width: 12px; height: 12px; vertical-align: middle; margin-right: 2px; opacity: 0.7;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"></path></svg>' : '';
+            const shareIcon = isShared ? '<svg title="Shared" style="display: inline-block; width: 12px; height: 12px; vertical-align: middle; margin-inline-end: 2px; opacity: 0.7;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"></path></svg>' : '';
             const icon = this.getMediaIcon(note.type);
             
             return `
@@ -1979,13 +1994,13 @@ function noteApp() {
                     onmouseover="window.$root.handleItemHover(this, true)"
                     onmouseout="window.$root.handleItemHover(this, false)"
                 >
-                    <span class="truncate" style="display: block; padding-right: 30px;" title="${esc(note.name)}">${shareIcon}${icon}${icon ? ' ' : ''}${esc(note.name)}</span>
+                    <span class="truncate" dir="auto" style="display: block; padding-inline-end: 30px;" title="${esc(note.name)}">${shareIcon}${icon}${icon ? ' ' : ''}${esc(note.name)}</span>
                     <button 
                         data-path="${esc(note.path)}"
                         data-name="${esc(note.name)}"
                         data-type="${note.type}"
                         onclick="window.$root.handleDeleteItemClick(this, event)"
-                        class="note-delete-btn absolute right-2 top-1/2 transform -translate-y-1/2 px-1 py-0.5 text-xs rounded hover:brightness-110 transition-opacity"
+                        class="note-delete-btn absolute end-2 top-1/2 transform -translate-y-1/2 px-1 py-0.5 text-xs rounded hover:brightness-110 transition-opacity"
                         style="opacity: 0; color: var(--error);"
                         title="${isMediaFile ? 'Delete file' : 'Delete note'}"
                     >
@@ -3969,7 +3984,7 @@ function noteApp() {
                         console.error('Mermaid rendering error:', error);
                         // Add error indicator to the code block
                         const errorMsg = document.createElement('div');
-                        errorMsg.style.cssText = 'color: var(--error); padding: 10px; border-left: 3px solid var(--error); margin-top: 10px;';
+                        errorMsg.style.cssText = 'color: var(--error); padding: 10px; border-inline-start: 3px solid var(--error); margin-top: 10px;';
                         errorMsg.textContent = `⚠️ Mermaid diagram error: ${error.message}`;
                         pre.parentElement.insertBefore(errorMsg, pre.nextSibling);
                     }
@@ -4331,7 +4346,7 @@ function noteApp() {
             // Style the button
             button.style.position = 'absolute';
             button.style.top = '8px';
-            button.style.right = '8px';
+            button.style.insetInlineEnd = '8px';
             button.style.padding = '4px 10px';
             button.style.backgroundColor = 'rgba(0, 0, 0, 0.6)';
             button.style.border = 'none';
@@ -4890,8 +4905,9 @@ function noteApp() {
             const resize = (e) => {
                 if (!this.isResizing) return;
                 
-                // Calculate new width based on mouse position
-                const newWidth = e.clientX;
+                // Calculate new width based on mouse position (RTL-aware)
+                const isRTL = document.documentElement.dir === 'rtl';
+                const newWidth = isRTL ? (window.innerWidth - e.clientX) : e.clientX;
                 
                 // Clamp between min and max
                 if (newWidth >= 200 && newWidth <= 600) {
@@ -4924,7 +4940,10 @@ function noteApp() {
                 
                 const containerRect = container.getBoundingClientRect();
                 const mouseX = e.clientX - containerRect.left;
-                const percentage = (mouseX / containerRect.width) * 100;
+                let percentage = (mouseX / containerRect.width) * 100;
+                // In RTL, editor is on the right, so invert the percentage
+                const isRTL = document.documentElement.dir === 'rtl';
+                if (isRTL) percentage = 100 - percentage;
                 
                 // Clamp between 20% and 80%
                 if (percentage >= 20 && percentage <= 80) {
@@ -5154,8 +5173,9 @@ function noteApp() {
                 }
                 
                 // Create standalone HTML document with MathJax
+                const exportDir = this.isRTL ? 'rtl' : 'ltr';
                 const htmlDocument = `<!DOCTYPE html>
-<html lang="en">
+<html lang="${this.currentLocale}" dir="${exportDir}">
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
@@ -5246,8 +5266,7 @@ function noteApp() {
             margin: 0;
             padding: 2rem;
             max-width: 900px;
-            margin-left: auto;
-            margin-right: auto;
+            margin-inline: auto;
             background-color: var(--bg-primary);
             color: var(--text-primary);
         }
diff --git a/frontend/index.html b/frontend/index.html
index 7d862f3..5877cd0 100644
--- a/frontend/index.html
+++ b/frontend/index.html
@@ -1,5 +1,5 @@
 <!DOCTYPE html>
-<html lang="en">
+<html :lang="typeof currentLocale !== 'undefined' ? currentLocale : 'en'" :dir="typeof isRTL !== 'undefined' && isRTL ? 'rtl' : 'ltr'" lang="en" dir="ltr">
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
@@ -253,8 +253,8 @@
         /* Lists */
         .markdown-preview ul, .markdown-preview ol { 
             margin-bottom: 1rem;
-            margin-left: 1.5rem;
-            padding-left: 0.5rem;
+            margin-inline-start: 1.5rem;
+            padding-inline-start: 0.5rem;
         }
         .markdown-preview ul { list-style-type: disc; }
         .markdown-preview ol { list-style-type: decimal; }
@@ -298,14 +298,14 @@
         
         /* Blockquotes */
         .markdown-preview blockquote { 
-            border-left: 4px solid var(--accent-primary);
-            padding-left: 1rem;
+            border-inline-start: 4px solid var(--accent-primary);
+            padding-inline-start: 1rem;
             margin: 1.5rem 0;
             font-style: italic;
             color: var(--text-secondary);
             background-color: var(--bg-tertiary);
             padding: 1rem;
-            padding-left: 1rem;
+            padding-inline-start: 1rem;
             border-radius: 0.25rem;
         }
         .markdown-preview blockquote p {
@@ -395,7 +395,7 @@
         .graph-legend {
             position: absolute;
             top: 10px;
-            right: 10px;
+            inset-inline-end: 10px;
             padding: 8px 12px;
             border-radius: 8px;
             font-size: 12px;
@@ -430,7 +430,7 @@
         .markdown-preview th, .markdown-preview td { 
             border: 1px solid var(--border-primary);
             padding: 0.75rem 1rem;
-            text-align: left;
+            text-align: start;
         }
         .markdown-preview th { 
             background-color: var(--bg-tertiary);
@@ -544,7 +544,7 @@
         
         /* Task lists */
         .markdown-preview input[type="checkbox"] {
-            margin-right: 0.5rem;
+            margin-inline-end: 0.5rem;
         }
         
         /* Math/LaTeX styling */
@@ -670,7 +670,7 @@
         .zen-exit-button {
             position: fixed;
             bottom: 2rem;
-            right: 2rem;
+            inset-inline-end: 2rem;
             padding: 0.75rem 1.25rem;
             border-radius: 9999px;
             background-color: var(--bg-tertiary);
@@ -726,13 +726,13 @@
         .folder-item {
             user-select: none;
             border-radius: 6px;
-            border-left: 3px solid transparent;
-            margin-left: 2px;
+            border-inline-start: 3px solid transparent;
+            margin-inline-start: 2px;
         }
         
         .folder-item:hover {
             background-color: var(--bg-hover);
-            border-left-color: var(--accent-primary);
+            border-inline-start-color: var(--accent-primary);
         }
         
         .folder-item.drag-over {
@@ -746,39 +746,39 @@
         
         .note-item {
             border-radius: 6px;
-            border-left: 3px solid transparent;
-            margin-left: 2px;
+            border-inline-start: 3px solid transparent;
+            margin-inline-start: 2px;
         }
         
         .note-item:hover {
             background-color: var(--bg-hover);
-            border-left-color: var(--accent-primary);
+            border-inline-start-color: var(--accent-primary);
             transform: translateX(2px);
         }
         
         /* Active/selected note styling */
         .note-item.active,
         .note-item[style*="accent-light"] {
-            border-left-color: var(--accent-primary) !important;
+            border-inline-start-color: var(--accent-primary) !important;
             font-weight: 500;
         }
         
         /* Shared hover accent effect for list items */
         .hover-accent {
             border-radius: 6px;
-            border-left: 3px solid transparent;
-            margin-left: 2px;
+            border-inline-start: 3px solid transparent;
+            margin-inline-start: 2px;
             transition: background-color 0.15s ease, border-color 0.15s ease, transform 0.1s ease;
         }
         
         .hover-accent:not(.active):hover {
             background-color: var(--bg-hover);
-            border-left-color: var(--accent-primary);
+            border-inline-start-color: var(--accent-primary);
             transform: translateX(2px);
         }
         
         .hover-accent.active {
-            border-left-color: var(--accent-primary);
+            border-inline-start-color: var(--accent-primary);
         }
         
         .hover-accent.active:hover {
@@ -787,12 +787,12 @@
         
         /* Tag chip - same effect but no margin */
         .tag-chip {
-            border-left: 3px solid transparent;
+            border-inline-start: 3px solid transparent;
             transition: border-color 0.15s ease, transform 0.1s ease;
         }
         
         .tag-chip:hover {
-            border-left-color: var(--accent-primary);
+            border-inline-start-color: var(--accent-primary);
             transform: translateX(2px);
         }
         
@@ -836,14 +836,14 @@
         /* Folder hierarchy lines (Obsidian-style) */
         .folder-contents {
             position: relative;
-            padding-left: 1rem;
-            margin-left: 0.5rem;
+            padding-inline-start: 1rem;
+            margin-inline-start: 0.5rem;
         }
         
         .folder-contents::before {
             content: '';
             position: absolute;
-            left: 2px;
+            inset-inline-start: 2px;
             top: 0;
             bottom: 0;
             width: 1px;
@@ -860,7 +860,7 @@
             align-items: center;
             padding: 8px 0;
             background-color: var(--bg-tertiary);
-            border-right: 1px solid var(--border-primary);
+            border-inline-end: 1px solid var(--border-primary);
         }
         
         .icon-rail-btn {
@@ -1221,19 +1221,19 @@
             /* Hide sidebar by default on mobile */
             .mobile-sidebar {
                 position: fixed;
-                left: -100%;
+                inset-inline-start: -100%;
                 top: 0;
                 height: 100vh;
                 height: 100dvh; /* Dynamic viewport height for iOS Safari */
                 width: 80% !important;
                 max-width: 300px !important;
                 z-index: 1000;
-                transition: left 0.3s ease;
+                transition: inset-inline-start 0.3s ease;
             }
             
             /* Show sidebar when open */
             .mobile-sidebar-open {
-                left: 0;
+                inset-inline-start: 0;
             }
             
             /* Add bottom padding to scrollable sidebar content for tab bar */
@@ -1331,6 +1331,31 @@
                 min-height: 140px;
             }
         }
+        
+        /* ==================== RTL Overrides ==================== */
+        /* Properties that cannot use CSS Logical Properties (translateX, gradients) */
+        [dir="rtl"] .note-item:hover {
+            transform: translateX(-2px);
+        }
+        [dir="rtl"] .hover-accent:not(.active):hover {
+            transform: translateX(-2px);
+        }
+        [dir="rtl"] .hover-accent.active:hover {
+            transform: translateX(-2px);
+        }
+        [dir="rtl"] .tag-chip:hover {
+            transform: translateX(-2px);
+        }
+        
+        /* RTL: Mirror directional icons (chevrons, arrows) */
+        [dir="rtl"] .rtl-mirror {
+            transform: scaleX(-1);
+        }
+        
+        /* RTL: Folder hover buttons gradient direction */
+        [dir="rtl"] .hover-buttons {
+            background: linear-gradient(to left, transparent, var(--bg-hover) 20%, var(--bg-hover)) !important;
+        }
     </style>
 </head>
 <body x-data="noteApp()" x-init="init()" style="background-color: var(--bg-primary);" :class="{'zen-mode-active': zenMode}">
@@ -1406,7 +1431,7 @@
                     <span 
                         x-show="searchQuery.trim() && searchResults.length > 0"
                         x-cloak
-                        class="absolute -top-1 -right-1 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
+                        class="absolute -top-1 -end-1 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
                         style="background-color: var(--accent-primary); color: white;"
                         x-text="searchResults.length > 9 ? '9+' : searchResults.length"
                     ></span>
@@ -1427,7 +1452,7 @@
                     <span 
                         x-show="selectedTags.length > 0"
                         x-cloak
-                        class="absolute -top-1 -right-1 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
+                        class="absolute -top-1 -end-1 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
                         style="background-color: var(--accent-primary); color: white;"
                         x-text="searchResults.length > 9 ? '9+' : searchResults.length"
                     ></span>
@@ -1448,7 +1473,7 @@
                     <span 
                         x-show="outline.length > 0"
                         x-cloak
-                        class="absolute -top-1 -right-1 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
+                        class="absolute -top-1 -end-1 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
                         style="background-color: var(--accent-primary); color: white;"
                         x-text="outline.length > 9 ? '9+' : outline.length"
                     ></span>
@@ -1485,7 +1510,7 @@
                     </div>
                     
             <!-- Sidebar Panel -->
-            <div class="sidebar-panel" style="border-right: 1px solid var(--border-primary);">
+            <div class="sidebar-panel" style="border-inline-end: 1px solid var(--border-primary);">
                 
                 <!-- ==================== FILES PANEL ==================== -->
                 <div x-show="activePanel === 'files'" x-transition:enter="transition ease-out duration-150" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" class="flex flex-col h-full">
@@ -1502,7 +1527,7 @@
                             <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                 <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                     </svg>
-                            New
+                            <span x-text="t('sidebar.new_button').replace('+ ', '')"></span>
                 </button>
             </div>
             
@@ -1542,7 +1567,7 @@
                                             <svg class="w-3.5 h-3.5 flex-shrink-0" style="color: var(--warning, #eab308);" fill="currentColor" viewBox="0 0 24 24">
                                                 <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"></path>
                                             </svg>
-                                            <span class="text-xs truncate" x-text="fav.name" :title="fav.name"></span>
+                                            <span class="text-xs truncate" dir="auto" x-text="fav.name" :title="fav.name"></span>
                                         </div>
                                         <button 
                                             @click.stop="toggleFavorite(fav.path)"
@@ -1638,19 +1663,20 @@
                         <div class="relative">
                             <input 
                                 type="text" 
+                                dir="auto"
                                 x-model="searchQuery"
                                 @input="debouncedSearchNotes()"
                                 :placeholder="t('sidebar.search_placeholder')"
-                                class="w-full px-2 py-1.5 pl-7 pr-7 text-xs rounded focus:outline-none focus:ring-1"
+                                class="w-full px-2 py-1.5 ps-7 pe-7 text-xs rounded focus:outline-none focus:ring-1"
                                 style="background-color: var(--bg-primary); color: var(--text-primary); border: 1px solid var(--border-primary);"
                             >
-                            <svg class="absolute left-2 top-2 w-3.5 h-3.5" style="color: var(--text-tertiary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                            <svg class="absolute start-2 top-2 w-3.5 h-3.5" style="color: var(--text-tertiary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                 <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                             </svg>
                             <button
                                 x-show="searchQuery.length > 0"
                                 @click="searchQuery = ''; searchResults = []; currentSearchHighlight = ''; clearSearchHighlights();"
-                                class="absolute right-1.5 top-1.5 w-4 h-4 flex items-center justify-center rounded-full hover:bg-gray-300 dark:hover:bg-gray-600 transition-colors"
+                                class="absolute end-1.5 top-1.5 w-4 h-4 flex items-center justify-center rounded-full hover:bg-gray-300 dark:hover:bg-gray-600 transition-colors"
                                 style="color: var(--text-tertiary);"
                                 :title="t('sidebar.clear_search')"
                             >
@@ -1669,10 +1695,10 @@
                             <span x-text="t('search.match_of', { current: currentMatchIndex + 1, total: totalMatches })"></span>
                             <div class="flex gap-1">
                                 <button @click="previousMatch()" class="px-2 py-1 rounded hover:bg-opacity-80" style="background-color: var(--bg-primary); color: var(--text-primary);" :title="t('search.previous')">
-                                    <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
+                                    <svg class="w-3 h-3 rtl-mirror" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path></svg>
                                 </button>
                                 <button @click="nextMatch()" class="px-2 py-1 rounded hover:bg-opacity-80" style="background-color: var(--bg-primary); color: var(--text-primary);" :title="t('search.next')">
-                                    <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
+                                    <svg class="w-3 h-3 rtl-mirror" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path></svg>
                                 </button>
                             </div>
                         </div>
@@ -1692,8 +1718,8 @@
                                         :class="{'active': currentNote === note.path}"
                                         :style="currentNote === note.path ? 'background-color: var(--accent-light); color: var(--accent-primary);' : 'color: var(--text-primary);'"
                                     >
-                                        <div class="font-medium truncate" x-text="note.name" :title="note.name"></div>
-                                        <div class="text-xs truncate" style="color: var(--text-tertiary);" x-html="(note.matches && note.matches.length > 0) ? note.matches[0].context : (note.folder || 'Root')" :title="note.folder || 'Root'"></div>
+                                        <div class="font-medium truncate" dir="auto" x-text="note.name" :title="note.name"></div>
+                                        <div class="text-xs truncate" dir="auto" style="color: var(--text-tertiary);" x-html="(note.matches && note.matches.length > 0) ? note.matches[0].context : (note.folder || 'Root')" :title="note.folder || 'Root'"></div>
                     </div>
                 </template>
             </div>
@@ -1761,7 +1787,7 @@
                                                 : 'background-color: var(--bg-tertiary); color: var(--text-primary);'"
                                             :title="t('tags.filter_by', {tag: tag, count: count})"
                                         >
-                                            <span x-text="tag"></span>
+                                            <span dir="auto" x-text="tag"></span>
                                             <span class="opacity-75" x-text="` (${count})`"></span>
                                         </button>
                                     </template>
@@ -1792,7 +1818,7 @@
                                             :class="{'active': currentNote === note.path}"
                                             :style="currentNote === note.path ? 'background-color: var(--accent-light); color: var(--accent-primary);' : 'color: var(--text-primary);'"
                                         >
-                                            <span class="truncate block" x-text="note.name" :title="note.name"></span>
+                                            <span class="truncate block" dir="auto" x-text="note.name" :title="note.name"></span>
                                         </div>
                                     </template>
                                     <div x-show="searchResults.length === 0" class="px-3 py-4 text-center text-xs" style="color: var(--text-tertiary);" x-text="t('tags.no_matches')">
@@ -1821,11 +1847,12 @@
                                     <template x-for="(heading, index) in outline" :key="index">
                                         <button 
                                             @click="scrollToHeading(heading)"
-                                            class="hover-accent w-full text-left px-3 py-1.5 text-sm"
-                                            :style="`padding-left: ${(heading.level - 1) * 12 + 12}px; color: var(--text-primary);`"
+                                            class="hover-accent w-full text-start px-3 py-1.5 text-sm"
+                                            :style="`padding-inline-start: ${(heading.level - 1) * 12 + 12}px; color: var(--text-primary);`"
                                         >
                                             <span 
                                                 class="block truncate"
+                                                dir="auto"
                                                 :style="heading.level === 1 ? 'font-weight: 600;' : (heading.level === 2 ? 'font-weight: 500;' : '')"
                                                 x-text="heading.text"
                                                 :title="heading.text"
@@ -1899,8 +1926,8 @@
                                             :style="syntaxHighlightEnabled ? 'background-color: var(--accent-primary)' : 'background-color: var(--bg-tertiary)'"
                                         >
                                             <div 
-                                                class="absolute top-0.5 left-0.5 w-4 h-4 rounded-full transition-transform"
-                                                :style="'background-color: var(--bg-primary);' + (syntaxHighlightEnabled ? ' transform: translateX(20px);' : '')"
+                                                class="absolute top-0.5 start-0.5 w-4 h-4 rounded-full transition-transform"
+                                                :style="'background-color: var(--bg-primary);' + (syntaxHighlightEnabled ? (isRTL ? ' transform: translateX(-20px);' : ' transform: translateX(20px);') : '')"
                                             ></div>
                                         </div>
                                     </label>
@@ -1917,8 +1944,8 @@
                                             :style="readableLineLength ? 'background-color: var(--accent-primary)' : 'background-color: var(--bg-tertiary)'"
                                         >
                                             <div 
-                                                class="absolute top-0.5 left-0.5 w-4 h-4 rounded-full transition-transform"
-                                                :style="'background-color: var(--bg-primary);' + (readableLineLength ? ' transform: translateX(20px);' : '')"
+                                                class="absolute top-0.5 start-0.5 w-4 h-4 rounded-full transition-transform"
+                                                :style="'background-color: var(--bg-primary);' + (readableLineLength ? (isRTL ? ' transform: translateX(-20px);' : ' transform: translateX(20px);') : '')"
                                             ></div>
                                         </div>
                                     </label>
@@ -1935,8 +1962,8 @@
                                             :style="hideUnderscoreFolders ? 'background-color: var(--accent-primary)' : 'background-color: var(--bg-tertiary)'"
                                         >
                                             <div 
-                                                class="absolute top-0.5 left-0.5 w-4 h-4 rounded-full transition-transform"
-                                                :style="'background-color: var(--bg-primary);' + (hideUnderscoreFolders ? ' transform: translateX(20px);' : '')"
+                                                class="absolute top-0.5 start-0.5 w-4 h-4 rounded-full transition-transform"
+                                                :style="'background-color: var(--bg-primary);' + (hideUnderscoreFolders ? (isRTL ? ' transform: translateX(-20px);' : ' transform: translateX(20px);') : '')"
                                             ></div>
                                         </div>
                                     </label>
@@ -2056,12 +2083,12 @@
                 <!-- Close button -->
                 <button 
                     @click="showGraph = false"
-                    class="absolute top-4 left-4 px-3 py-2 text-sm font-medium rounded-lg transition-colors z-10"
+                    class="absolute top-4 start-4 px-3 py-2 text-sm font-medium rounded-lg transition-colors z-10"
                     style="background-color: var(--bg-secondary); color: var(--text-primary); border: 1px solid var(--border-primary);"
                     onmouseover="this.style.backgroundColor='var(--bg-hover)'"
                     onmouseout="this.style.backgroundColor='var(--bg-secondary)'"
                 >
-                    ← Back
+                    <span x-text="isRTL ? 'Back →' : '← Back'"></span>
                 </button>
             </div>
             
@@ -2113,7 +2140,7 @@
                                 <p class="text-lg" style="color: var(--text-secondary);" x-text="t('app.tagline')"></p>
                                 <!-- Demo Mode Warning Banner -->
                                 <div x-show="demoMode" class="mt-4 flex items-center px-4 py-2.5 text-sm rounded-lg" style="background: linear-gradient(135deg, #ff6b6b 0%, #ff8e53 100%); color: white; font-weight: 500;">
-                                    <svg class="w-5 h-5 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                                    <svg class="w-5 h-5 me-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                         <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                                     </svg>
                                     <span><strong x-text="t('demo.title') + ':'"></strong> <span x-text="t('demo.warning')"></span></span>
@@ -2133,6 +2160,7 @@
                                             <span 
                                                 @click="goToHomepageFolder(crumb.path)"
                                                 class="cursor-pointer hover:underline"
+                                                dir="auto"
                                                 :style="crumb.path === selectedHomepageFolder ? 'color: var(--accent-primary); font-weight: 600;' : 'color: var(--text-secondary);'"
                                                 x-text="crumb.name"
                                             ></span>
@@ -2155,7 +2183,7 @@
                                         </span>
                                         <button 
                                             @click="dropdownTargetFolder = selectedHomepageFolder; toggleNewDropdown($event)"
-                                            class="ml-auto px-4 py-2 text-sm font-medium text-white rounded-lg transition-colors"
+                                            class="ms-auto px-4 py-2 text-sm font-medium text-white rounded-lg transition-colors"
                                             style="background-color: var(--accent-primary);"
                                             onmouseover="this.style.backgroundColor='var(--accent-hover)'"
                                             onmouseout="this.style.backgroundColor='var(--accent-primary)'"
@@ -2202,7 +2230,7 @@
                                             <button 
                                                 @click.stop="deleteFolder(folder.path, folder.name)"
                                                 class="card-delete-btn hidden sm:block"
-                                                style="position: absolute; top: 8px; right: 8px; opacity: 0; transition: opacity 0.2s; color: var(--error); padding: 4px; border-radius: 4px; background-color: var(--bg-secondary);"
+                                                style="position: absolute; top: 8px; inset-inline-end: 8px; opacity: 0; transition: opacity 0.2s; color: var(--error); padding: 4px; border-radius: 4px; background-color: var(--bg-secondary);"
                                                 onmouseover="this.style.backgroundColor='var(--bg-hover)'"
                                                 onmouseout="this.style.backgroundColor='var(--bg-secondary)'"
                                                 :title="t('common.delete')"
@@ -2217,13 +2245,13 @@
                                                 <svg class="w-5 h-5 sm:w-6 sm:h-6 flex-shrink-0" style="color: var(--accent-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                     <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"></path>
                                                 </svg>
-                                                <h3 class="font-semibold text-sm sm:text-base" style="color: var(--text-primary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap;" x-text="folder.name"></h3>
+                                                <h3 class="font-semibold text-sm sm:text-base" dir="auto" style="color: var(--text-primary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap;" x-text="folder.name"></h3>
                                             </div>
                                             
                                             <!-- Folder Metadata -->
                                             <div class="flex items-center justify-between text-xs mt-auto pt-1 sm:pt-2" style="color: var(--text-tertiary);">
                                                 <span x-text="folder.noteCount + ' ' + (folder.noteCount === 1 ? t('homepage.note_singular') : t('homepage.note_plural'))"></span>
-                                                <svg class="w-4 h-4 hidden sm:block" style="color: var(--accent-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                                                <svg class="w-4 h-4 hidden sm:block rtl-mirror" style="color: var(--accent-primary);" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                     <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                                                 </svg>
                                             </div>
@@ -2243,7 +2271,7 @@
                                             <button 
                                                 @click.stop="note.type !== 'note' ? deleteMedia(note.path) : deleteNote(note.path, note.name)"
                                                 class="card-delete-btn hidden sm:block"
-                                                style="position: absolute; top: 8px; right: 8px; opacity: 0; transition: opacity 0.2s; color: var(--error); padding: 4px; border-radius: 4px; background-color: var(--bg-secondary);"
+                                                style="position: absolute; top: 8px; inset-inline-end: 8px; opacity: 0; transition: opacity 0.2s; color: var(--error); padding: 4px; border-radius: 4px; background-color: var(--bg-secondary);"
                                                 onmouseover="this.style.backgroundColor='var(--bg-hover)'"
                                                 onmouseout="this.style.backgroundColor='var(--bg-secondary)'"
                                                 :title="note.type !== 'note' ? t('toolbar.delete_image') : t('toolbar.delete_note')"
@@ -2254,7 +2282,7 @@
                                             </button>
                                             
                                             <!-- Note Title -->
-                                            <h3 class="font-semibold text-sm sm:text-base mb-1 sm:mb-2" style="color: var(--text-primary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap;" x-text="note.name"></h3>
+                                            <h3 class="font-semibold text-sm sm:text-base mb-1 sm:mb-2" dir="auto" style="color: var(--text-primary); overflow: hidden; text-overflow: ellipsis; white-space: nowrap;" x-text="note.name"></h3>
                                             
                                             <!-- Metadata -->
                                             <div class="flex items-center justify-between text-xs mt-auto pt-1 sm:pt-2" style="color: var(--text-tertiary);">
@@ -2282,7 +2310,7 @@
                 <div class="flex-1 flex flex-col overflow-hidden" :class="{'zen-editor-container': zenMode}" style="background-color: var(--bg-primary);">
                     <!-- Toolbar -->
                     <div class="px-4 py-3 flex items-center justify-between mobile-toolbar zen-hide" style="background-color: var(--bg-secondary); border-bottom: 1px solid var(--border-primary);">
-                        <div class="flex items-center space-x-2">
+                        <div class="flex items-center gap-2">
                             <!-- Mobile Menu Button -->
                             <button 
                                 @click="mobileSidebarOpen = !mobileSidebarOpen"
@@ -2298,6 +2326,7 @@
                             </button>
                             <input 
                                 type="text" 
+                                dir="auto"
                                 :value="currentNote ? currentNoteName : (currentMedia ? currentMedia.split('/').pop() : '')"
                                 @input="if (currentNote) currentNoteName = $event.target.value"
                                 @blur="if (currentNote) renameNote()"
@@ -2355,13 +2384,13 @@
                         
                         <!-- Demo Mode Warning Banner -->
                         <div x-show="demoMode" class="flex items-center px-3 py-1.5 text-xs rounded-lg" style="background: linear-gradient(135deg, #ff6b6b 0%, #ff8e53 100%); color: white; font-weight: 500;">
-                            <svg class="w-4 h-4 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                            <svg class="w-4 h-4 me-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                 <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                             </svg>
                             <span><strong x-text="t('demo.title') + ':'"></strong> <span x-text="t('demo.warning')"></span></span>
                         </div>
                         
-                        <div class="flex items-center space-x-2" x-show="currentNote">
+                        <div class="flex items-center gap-2" x-show="currentNote">
                             <!-- View Toggle (only for notes) -->
                             <div class="flex rounded-lg p-1" style="background-color: var(--bg-tertiary);">
                                 <button 
@@ -2541,11 +2570,13 @@
                                 <div 
                                     id="syntax-overlay"
                                     class="syntax-overlay"
+                                    dir="auto"
                                     x-show="syntaxHighlightEnabled"
                                     x-html="syntaxHighlightEnabled ? highlightMarkdown(noteContent) : ''"
                                 ></div>
                                 <textarea 
                                     id="note-editor"
+                                    dir="auto"
                                     x-model="noteContent"
                                     @input="autoSave(); updateSyntaxHighlight()"
                                     @scroll="syncOverlayScroll()"
@@ -2600,6 +2631,7 @@
                                         <span 
                                             @click.stop="toggleTag(tag.toLowerCase())"
                                             class="metadata-tag px-2 py-0.5 text-xs rounded-full cursor-pointer transition-colors"
+                                            dir="auto"
                                             style="background-color: var(--accent-light, rgba(124, 58, 237, 0.15)); color: var(--accent-primary);"
                                             onmouseover="this.style.backgroundColor='var(--accent-primary)'; this.style.color='white'"
                                             onmouseout="this.style.backgroundColor='var(--accent-light, rgba(124, 58, 237, 0.15))'; this.style.color='var(--accent-primary)'"
@@ -2615,12 +2647,12 @@
                                         <span class="text-xs flex items-center gap-1" style="color: var(--text-secondary);">
                                             <span class="opacity-50">·</span>
                                             <span class="capitalize" x-text="field.key + ':'"></span>
-                                            <span style="color: var(--text-primary);" x-html="field.valueHtml" @click="field.isUrl && $event.stopPropagation()"></span>
+                                            <span dir="auto" style="color: var(--text-primary);" x-html="field.valueHtml" @click="field.isUrl && $event.stopPropagation()"></span>
                                         </span>
                                     </template>
                                     
                                     <!-- Expand hint -->
-                                    <span class="ml-auto text-xs opacity-40">▼</span>
+                                    <span class="ms-auto text-xs opacity-40">▼</span>
                                 </div>
                                 
                                 <!-- Expanded View -->
@@ -2652,6 +2684,7 @@
                                                         <span 
                                                             @click="toggleTag(tag.toLowerCase())"
                                                             class="metadata-tag px-2 py-0.5 text-xs rounded-full cursor-pointer transition-colors"
+                                                            dir="auto"
                                                             style="background-color: var(--accent-light, rgba(124, 58, 237, 0.15)); color: var(--accent-primary);"
                                                             onmouseover="this.style.backgroundColor='var(--accent-primary)'; this.style.color='white'"
                                                             onmouseout="this.style.backgroundColor='var(--accent-light, rgba(124, 58, 237, 0.15))'; this.style.color='var(--accent-primary)'"
@@ -2672,6 +2705,7 @@
                                                         <template x-for="item in field.value" :key="item">
                                                             <span 
                                                                 class="px-1.5 py-0.5 rounded text-xs"
+                                                                dir="auto"
                                                                 style="background-color: var(--bg-hover); color: var(--text-primary);"
                                                                 x-text="item"
                                                             ></span>
@@ -2680,7 +2714,7 @@
                                                 </template>
                                                 <!-- Single values -->
                                                 <template x-if="!field.isArray">
-                                                    <span style="color: var(--text-primary);" x-html="field.valueHtml"></span>
+                                                    <span dir="auto" style="color: var(--text-primary);" x-html="field.valueHtml"></span>
                                                 </template>
                                             </div>
                                         </template>
@@ -2690,6 +2724,7 @@
                             
                             <div 
                                 class="p-6 markdown-preview"
+                                dir="auto"
                                 :class="{ 'pt-2': getHasMetadata() && currentNote, 'readable-width': readableLineLength }"
                                 x-html="renderedMarkdown"
                                 @click="handleInternalLink($event)"
@@ -2760,7 +2795,7 @@
                              class="px-4 py-2 flex items-center justify-between cursor-pointer hover:bg-opacity-80 transition-colors"
                              onmouseover="this.style.backgroundColor='var(--bg-hover)'"
                              onmouseout="this.style.backgroundColor='transparent'">
-                            <div class="flex items-center space-x-4 text-sm" style="color: var(--text-secondary);">
+                            <div class="flex items-center gap-4 text-sm" style="color: var(--text-secondary);">
                                 <span class="font-medium">📊</span>
                                 <span x-text="noteStats ? noteStats.words.toLocaleString() + ' words' : ''"></span>
                                 <span>·</span>
@@ -2911,7 +2946,7 @@
         <div class="py-1">
             <button 
                 @click="createNote()"
-                class="w-full px-4 py-2.5 text-sm text-left flex items-center gap-3 transition-colors"
+                class="w-full px-4 py-2.5 text-sm text-start flex items-center gap-3 transition-colors"
                 style="color: var(--text-primary);"
                 onmouseover="this.style.backgroundColor='var(--bg-hover)'"
                 onmouseout="this.style.backgroundColor='transparent'"
@@ -2921,7 +2956,7 @@
             </button>
             <button 
                 @click="createFolder()"
-                class="w-full px-4 py-2.5 text-sm text-left flex items-center gap-3 transition-colors"
+                class="w-full px-4 py-2.5 text-sm text-start flex items-center gap-3 transition-colors"
                 style="color: var(--text-primary);"
                 onmouseover="this.style.backgroundColor='var(--bg-hover)'"
                 onmouseout="this.style.backgroundColor='transparent'"
@@ -2931,7 +2966,7 @@
             </button>
             <button 
                 @click.stop="showTemplateModal = true; showNewDropdown = false; mobileSidebarOpen = false"
-                class="w-full px-4 py-2.5 text-sm text-left flex items-center gap-3 transition-colors"
+                class="w-full px-4 py-2.5 text-sm text-start flex items-center gap-3 transition-colors"
                 style="color: var(--text-primary);"
                 onmouseover="this.style.backgroundColor='var(--bg-hover)'"
                 onmouseout="this.style.backgroundColor='transparent'"
@@ -2961,6 +2996,7 @@
                 </label>
                 <select 
                     x-model="selectedTemplate"
+                    dir="auto"
                     class="w-full px-3 py-2 rounded border"
                     style="background-color: var(--bg-tertiary); color: var(--text-primary); border-color: var(--border-primary);"
                 >
@@ -2978,6 +3014,7 @@
                 <input 
                     x-model="newTemplateNoteName"
                     type="text"
+                    dir="auto"
                     :placeholder="t('notes.prompt_name')"
                     class="w-full px-3 py-2 rounded border"
                     style="background-color: var(--bg-tertiary); color: var(--text-primary); border-color: var(--border-primary);"
@@ -3044,6 +3081,7 @@
             <div class="p-3 border-b" style="border-color: var(--border-primary);">
                 <input id="quickSwitcherInput"
                        type="text"
+                       dir="auto"
                        x-model="quickSwitcherQuery"
                        @keydown="handleQuickSwitcherKeydown($event)"
                        :placeholder="t('quick_switcher.placeholder')"
@@ -3071,8 +3109,8 @@
                             <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                         </svg>
                         <div class="flex-1 min-w-0">
-                            <div class="font-medium truncate" style="color: var(--text-primary);" x-text="note.name" :title="note.name"></div>
-                            <div class="text-xs truncate" style="color: var(--text-secondary);" x-show="note.folder" x-text="note.folder" :title="note.folder"></div>
+                            <div class="font-medium truncate" dir="auto" style="color: var(--text-primary);" x-text="note.name" :title="note.name"></div>
+                            <div class="text-xs truncate" dir="auto" style="color: var(--text-secondary);" x-show="note.folder" x-text="note.folder" :title="note.folder"></div>
                         </div>
                     </div>
                 </template>
@@ -3250,7 +3288,7 @@
             <span 
                 x-show="searchQuery.trim() && searchResults.length > 0"
                 x-cloak
-                class="absolute top-1 right-1/4 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
+                class="absolute top-1 end-1/4 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
                 style="background-color: var(--accent-primary); color: white;"
                 x-text="searchResults.length > 9 ? '9+' : searchResults.length"
             ></span>
@@ -3269,7 +3307,7 @@
             <span 
                 x-show="selectedTags.length > 0"
                 x-cloak
-                class="absolute top-1 right-1/4 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
+                class="absolute top-1 end-1/4 w-4 h-4 text-[9px] font-bold rounded-full flex items-center justify-center"
                 style="background-color: var(--accent-primary); color: white;"
                 x-text="searchResults.length > 9 ? '9+' : searchResults.length"
             ></span>
diff --git a/frontend/login.html b/frontend/login.html
index 8e0706e..f309bb6 100644
--- a/frontend/login.html
+++ b/frontend/login.html
@@ -1,5 +1,5 @@
 <!DOCTYPE html>
-<html lang="en">
+<html lang="en" dir="ltr">
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
@@ -44,6 +44,13 @@
                 }
                 return typeof value === 'string' ? value : key;
             };
+            
+            // Apply RTL direction if locale is RTL
+            const RTL_LOCALES = ['he-IL', 'ar-SA', 'ar-EG', 'fa-IR', 'ur-PK'];
+            if (RTL_LOCALES.includes(locale)) {
+                document.documentElement.setAttribute('dir', 'rtl');
+            }
+            document.documentElement.setAttribute('lang', locale);
         })();
     </script>
     
@@ -174,7 +181,7 @@
             color: var(--error, #c53030);
             font-size: 0.813rem;
             margin-top: 0.5rem;
-            text-align: left;
+            text-align: start;
             display: flex;
             align-items: center;
             gap: 0.375rem;
diff --git a/locales/he-IL.json b/locales/he-IL.json
new file mode 100644
index 0000000..5e779bf
--- /dev/null
+++ b/locales/he-IL.json
@@ -0,0 +1,295 @@
+{
+  "_meta": {
+    "code": "he-IL",
+    "name": "עברית",
+    "flag": "🇮🇱",
+    "dir": "rtl"
+  },
+
+  "app": {
+    "tagline": "מאגר הידע האישי שלך"
+  },
+
+  "common": {
+    "save": "שמור",
+    "cancel": "ביטול",
+    "delete": "מחק",
+    "rename": "שנה שם",
+    "create": "צור",
+    "close": "סגור",
+    "yes": "✓ כן",
+    "no": "✗ לא",
+    "ok": "אישור",
+    "error": "שגיאה",
+    "loading": "טוען...",
+    "saved": "✓ נשמר",
+    "saving": "שומר...",
+    "copied": "הועתק!",
+    "copy_to_clipboard": "העתק ללוח",
+    "failed": "הפעולה {{action}} נכשלה. נסה שוב."
+  },
+
+  "sidebar": {
+    "title": "קבצים",
+    "favorites_title": "מועדפים",
+    "folders_and_notes": "תיקיות ופתקים",
+    "new_button": "+ חדש",
+    "new_note": "פתק חדש",
+    "new_folder": "תיקייה חדשה",
+    "new_from_template": "חדש מתבנית",
+    "search_placeholder": "חיפוש פתקים...",
+    "search_hint": "הקלד כדי לחפש בפתקים",
+    "clear_search": "נקה חיפוש",
+    "drag_hint": "💡 גרור=העבר",
+    "root_folder": "📂 תיקיית שורש",
+    "no_notes": "אין עדיין פתקים. צור את הפתק הראשון!",
+    "no_notes_yet": "אין עדיין פתקים או תיקיות.",
+    "create_first": "צור את הפתק או התיקייה הראשונים!",
+    "drop_to_root": "שחרר כאן לשורש",
+    "no_favorites": "אין עדיין מועדפים",
+    "no_results": "לא נמצאו תוצאות",
+    "expand_all": "הרחב את כל התיקיות",
+    "collapse_all": "כווץ את כל התיקיות",
+    "toggle_sidebar": "הצג/הסתר סרגל צד",
+    "go_to_homepage": "עבור לדף הבית",
+    "files": "קבצים",
+    "search": "חיפוש",
+    "search_title": "חיפוש",
+    "settings": "הגדרות",
+    "settings_title": "הגדרות",
+    "filtered_notes": "פתקים מסוננים"
+  },
+
+  "editor": {
+    "placeholder": "התחל לכתוב ב-Markdown...",
+    "drop_hint": "💡 שחרר כאן כדי להוסיף במיקום הסמן...",
+    "mode_edit": "עריכה",
+    "mode_split": "מפוצל",
+    "mode_preview": "תצוגה מקדימה",
+    "edited": "נערך {{time}}",
+    "just_now": "הרגע",
+    "minutes_ago": "לפני {{count}} דקות",
+    "hours_ago": "לפני {{count}} שעות",
+    "days_ago": "לפני {{count}} ימים"
+  },
+
+  "notes": {
+    "confirm_delete": "למחוק את \"{{name}}\"?",
+    "already_exists": "פתק בשם \"{{name}}\" כבר קיים במיקום זה.\nבחר שם אחר.",
+    "prompt_name": "הזן שם פתק:",
+    "prompt_name_in_folder": "יצירת פתק בתיקייה \"{{folder}}\".\nהזן שם פתק:",
+    "prompt_name_with_path": "הזן שם פתק (ניתן להשתמש בתיקייה/שם):",
+    "prompt_rename": "הזן שם חדש:",
+    "invalid_name": "שם פתק לא חוקי.",
+    "empty_name": "שם הפתק לא יכול להיות ריק.",
+    "not_found": "הפתק לא נמצא: {{path}}",
+    "create_from_link": "הפתק \"{{path}}\" לא קיים.\n\nהאם תרצה ליצור אותו?",
+    "no_content": "אין תוכן פתק לייצוא",
+    "open_first": "פתח פתק לפני העלאת תמונות."
+  },
+
+  "folders": {
+    "confirm_delete": "⚠️ אזהרה ⚠️\n\nהאם אתה בטוח שברצונך למחוק את התיקייה \"{{name}}\"?\n\nפעולה זו תמחק לצמיתות:\n• את כל הפתקים בתיקייה\n• את כל תתי-התיקיות והתוכן שלהן\n\nלא ניתן לבטל פעולה זו!",
+    "already_exists": "תיקייה בשם \"{{name}}\" כבר קיימת במיקום זה.\nבחר שם אחר.",
+    "prompt_name": "הזן שם תיקייה:",
+    "prompt_name_in_folder": "יצירת תת-תיקייה ב-\"{{folder}}\".\nהזן שם תיקייה:",
+    "prompt_name_with_path": "יצירת תיקייה חדשה.\nהזן נתיב תיקייה (לדוגמה: \"פרויקטים\" או \"עבודה/2025\"):",
+    "prompt_rename": "שנה שם תיקייה \"{{name}}\" ל:",
+    "invalid_name": "שם תיקייה לא חוקי.",
+    "cannot_move_into_self": "לא ניתן להעביר תיקייה לתוך עצמה או לתוך תת-תיקייה שלה.",
+    "empty": "ריקה"
+  },
+
+  "toolbar": {
+    "undo": "בטל (Ctrl+Z)",
+    "redo": "בצע שוב (Ctrl+Y)",
+    "delete_note": "מחק פתק",
+    "delete_image": "מחק תמונה",
+    "export_html": "ייצא כ-HTML",
+    "copy_link": "העתק קישור ללוח",
+    "add_favorite": "הוסף למועדפים",
+    "remove_favorite": "הסר ממועדפים"
+  },
+
+  "zen_mode": {
+    "title": "מצב זן",
+    "tooltip": "מצב זן (Ctrl+Alt+Z)",
+    "exit": "צא ממצב זן",
+    "exit_hint": "צא ממצב זן (Esc)"
+  },
+
+  "share": {
+    "button_tooltip": "שתף פתק",
+    "modal_title": "שיתוף פתק",
+    "not_shared_description": "פתק זה אינו משותף כרגע. צור קישור כדי לשתף אותו עם כל אחד.",
+    "shared_description": "כל מי שיש לו קישור זה יכול לצפות בפתק:",
+    "create_link": "צור קישור שיתוף",
+    "copy": "העתק",
+    "stop_sharing": "הפסק שיתוף",
+    "confirm_revoke": "האם אתה בטוח שברצונך להפסיק לשתף פתק זה? קישור השיתוף יפסיק לעבוד.",
+    "error_creating": "יצירת קישור שיתוף נכשלה: {{error}}",
+    "error_revoking": "ביטול שיתוף נכשל: {{error}}",
+    "show_qr": "הצג קוד QR",
+    "hide_qr": "הסתר קוד QR"
+  },
+
+  "quick_switcher": {
+    "placeholder": "הקלד כדי לחפש פתקים...",
+    "no_results": "לא נמצאו פתקים תואמים",
+    "recent_hint": "מציג פתקים אחרונים. הקלד כדי לחפש בכולם.",
+    "navigate": "ניווט",
+    "open": "פתח",
+    "close": "סגור"
+  },
+
+  "tags": {
+    "title": "תגיות",
+    "no_tags": "לא נמצאו תגיות",
+    "hint": "הוסף תגיות באמצעות YAML frontmatter",
+    "no_matches": "אין פתקים שתואמים לתגיות שנבחרו",
+    "clear_all": "נקה סינון תגיות",
+    "filter_by": "סנן לפי {{tag}} ({{count}} פתקים)"
+  },
+
+  "outline": {
+    "title": "מתאר",
+    "no_headings": "לא נמצאו כותרות",
+    "hint": "הוסף כותרות באמצעות תחביר #"
+  },
+
+  "stats": {
+    "words": "מילים",
+    "reading_time": "~{{minutes}} דקות קריאה",
+    "links": "{{count}} קישורים",
+    "click_details": "▼ לחץ לפרטים"
+  },
+
+  "graph": {
+    "title": "תצוגת גרף",
+    "empty": "אין קשרים להציג",
+    "markdown_links": "קישורי Markdown",
+    "click_hint": "לחיצה: בחר • לחיצה כפולה: פתח"
+  },
+
+  "templates": {
+    "title": "תבניות",
+    "select": "בחר תבנית...",
+    "prompt_name": "הזן שם פתק:",
+    "no_templates": "לא נמצאו תבניות. צור תבניות בתיקיית _templates.",
+    "create_failed": "יצירת פתק מתבנית נכשלה",
+    "create_from_template": "צור מתבנית",
+    "select_template": "בחר תבנית",
+    "choose_template": "בחר תבנית",
+    "note_name": "שם פתק",
+    "available_placeholders": "מציינים זמינים",
+    "create_note": "צור פתק"
+  },
+
+  "export": {
+    "failed": "ייצוא HTML נכשל: {{error}}"
+  },
+
+  "login": {
+    "title": "התחברות",
+    "tagline": "מאגר הידע האישי שלך",
+    "password_placeholder": "הזן סיסמה",
+    "unlock_button": "🔓 פתח",
+    "footer": "🔒 מאובטח ועצמאי",
+    "error_incorrect_password": "סיסמה שגויה. נסה שוב."
+  },
+
+  "media": {
+    "confirm_delete": "למחוק את \"{{name}}\"?",
+    "upload_failed": "העלאת קובץ נכשלה",
+    "no_valid_files": "לא נמצאו קבצים תקינים. נתמכים: JPG, PNG, GIF, WebP, MP3, MP4, PDF",
+    "formats": "JPG, PNG, GIF, WebP, MP3, MP4, PDF"
+  },
+
+  "move": {
+    "failed_note": "העברת פתק נכשלה.",
+    "failed_folder": "העברת תיקייה נכשלה.",
+    "failed_media": "העברת קובץ מדיה נכשלה."
+  },
+
+  "search": {
+    "previous": "הקודם (Shift+F3)",
+    "next": "הבא (F3)",
+    "matches": "{{current}} מתוך {{total}}",
+    "match_of": "התאמה {{current}} מתוך {{total}}",
+    "result_singular": "תוצאה",
+    "result_plural": "תוצאות",
+    "no_results": "אין פתקים שמכילים \"{{query}}\"",
+    "searching": "מחפש \"{{query}}\"..."
+  },
+
+  "theme": {
+    "title": "ערכת נושא"
+  },
+
+  "language": {
+    "title": "שפה"
+  },
+
+  "syntax_highlight": {
+    "title": "הדגשת תחביר בעורך",
+    "description": "צביעת תחביר Markdown בעורך",
+    "enable": "הפעל הדגשת תחביר",
+    "disable": "השבת הדגשת תחביר"
+  },
+
+  "settings": {
+    "account": "חשבון",
+    "logout": "התנתק",
+    "readable_line_length": "רוחב שורה קריא",
+    "readable_line_length_desc": "הגבל את רוחב התצוגה המקדימה לקריאה נוחה",
+    "hide_underscore_folders": "הסתר תיקיות מערכת",
+    "hide_underscore_folders_desc": "הסתר את _attachments, _templates ותיקיות קו תחתון נוספות מהסרגל הצדדי"
+  },
+
+  "homepage": {
+    "title": "דף הבית",
+    "welcome": "ברוכים הבאים ל-NoteDiscovery",
+    "get_started": "צור משהו כדי להתחיל",
+    "no_notes_title": "אין עדיין פתקים",
+    "no_notes_desc": "צור את הפתק או התיקייה הראשונים",
+    "folder_empty": "תיקייה זו ריקה",
+    "note_singular": "פתק",
+    "note_plural": "פתקים",
+    "folder_singular": "תיקייה",
+    "folder_plural": "תיקיות"
+  },
+
+  "format": {
+    "bold": "מודגש",
+    "italic": "נטוי",
+    "strikethrough": "קו חוצה",
+    "heading": "כותרת",
+    "link": "קישור",
+    "image": "תמונה",
+    "code": "קוד בשורה",
+    "codeblock": "בלוק קוד",
+    "quote": "ציטוט",
+    "bullet_list": "רשימת תבליטים",
+    "numbered_list": "רשימה ממוספרת",
+    "checkbox": "תיבת סימון",
+    "table": "טבלה"
+  },
+
+  "validation": {
+    "forbidden_chars": "השם מכיל תווים אסורים: {{chars}}",
+    "reserved_name": "שם זה שמור על ידי מערכת ההפעלה.",
+    "invalid_dot": "השם לא יכול להיות רק נקודה.",
+    "trailing_dot_space": "השם לא יכול להסתיים בנקודה או ברווח."
+  },
+
+  "support": {
+    "enjoying_demo": "תמכו בפרויקט",
+    "deploy_own": "התקינו מופע משלכם",
+    "thank_you": "תודה על התמיכה! 💚"
+  },
+
+  "demo": {
+    "title": "מצב הדגמה",
+    "warning": "התכנים מתאפסים מדי יום. שינויים עלולים להידרס על ידי משתמשים אחרים."
+  }
+}
-- 
2.47.3

